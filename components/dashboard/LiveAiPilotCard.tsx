import{getLiveAiPilotStatus}from"@/app/actions/liveAiPilot";
export default async function LiveAiPilotCard(){const status=await getLiveAiPilotStatus();return <section className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold text-slate-900">Live AI pilot</h2><p className="text-sm text-slate-600">Content-free operational readiness</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${status?.enabled&&!status.killSwitch?"bg-emerald-100 text-emerald-800":"bg-amber-100 text-amber-800"}`}>{!status?"Unavailable":status.killSwitch?"Stopped":status.enabled?"Enabled":"Disabled"}</span></div>{status&&<dl className="mt-4 grid gap-3 text-sm sm:grid-cols-3"><div><dt className="text-slate-500">Provider / model</dt><dd className="font-medium">{status.provider} / {status.model}</dd></div><div><dt className="text-slate-500">Credential</dt><dd className="font-medium">{status.credentialConfigured?"Configured":"Missing"}</dd></div><div><dt className="text-slate-500">Token ceilings</dt><dd className="font-medium">{status.inputTokenLimit.toLocaleString()} in / {status.outputTokenLimit.toLocaleString()} out</dd></div><div><dt className="text-slate-500">Synthetic</dt><dd>{status.syntheticEnabled?"Allowed":"Denied"}</dd></div><div><dt className="text-slate-500">Non-confidential</dt><dd>{status.nonConfidentialEnabled?"Allowed":"Denied"}</dd></div><div><dt className="text-slate-500">Proposal sources</dt><dd>{status.proposalSourceEnabled?"Allowed":"Denied"}</dd></div><div><dt className="text-slate-500">Mutation / publication</dt><dd>Disabled / disabled</dd></div></dl>}</section>}
